# Tanki 2D (pygame)

Простая 2D-игра про танчики на Python с использованием библиотеки pygame.
Игрок управляет танком, перемещается по карте, стреляет, уничтожает врагов и объекты.

## Как играть
    В нашей игре имеется 2 режима: 
    Одиночный и мультиплеер. 
    В одиночном в режими вам предстоит сразится с армией врага на базе искусственного интелекта.
    В то время как в мультиплеере вы можете показать своим друзьям что не просто так оттачивали свои навыки против ботов.

## Как подключится в мультплеер

    В нашем проекте мультплеер реализован следующим образом.
    Каждый игрок может выступать в роле хоста, и к нему уже могут подключится остальные игроки.
    
    Следовательно чтобы поиграть с друзьями вам необходимо следующее:
    1) С помощью программы прокинуть свое подключение (если находитесь на расстоянии друг от друга). 
    Пример программ:
    - Windows: Radmin wpn
    - Linux: ZeroTier 
    1.1) Либо подключится к одной сети (Если находитесь рядом)
    2) Создать мир
    3) Подключится остальным игрокам. В поле IP-адресс необходимо вводить 4 цифры от 0 до 255 разделенные ".", посмотреть IP-адресс человека который создал мир можно в приложениях указанных выше.
    4) Насладится игрой

## 📁 Структура проекта
```
tanki/
├── main.py
├── settings.py
├── requirements.txt
│
├── assets/
│   ├── images/
│   └── sounds/
│
├── core/
│   ├── game.py
│   └── multiplayer.py
│
├── objects/
│   ├── bullet.py
│   ├── enemy_tank.py
│   └── tank.py
│
└── maps/
    └── map.txt
```

## 🔧 Установка
```
$ git clone https://github.com/ViktorLamba/Tanki-pygame-.git
$ cd tanki-pygame-
```
Далее создаем виртуальное окружение
```
$ python3 -m venv venv
$ source venv/bin/activate
```
Устанавливаем зависимости
```
$ venv/bin/python3 pip install -r requirements.txt
```